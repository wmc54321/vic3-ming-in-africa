# AGENTS.md

Read `docs/agent-handoff.md` first. It contains the current onboarding path, generated-file rules, local environment handling, and narrative premise.

Short version:

- Keep `README.md` as a public introduction, not an implementation dump.
- Put durable implementation notes in `docs/implementation-map.md`.
- Keep long design history in `docs/design-plan.md` and `docs/route-journal-design.md`.
- Store machine-specific Victoria 3 paths in `.env.local`; never hard-code them in docs.
- Work inside `mod/ming_in_africa` for loadable mod changes.
